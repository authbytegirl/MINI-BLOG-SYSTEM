services:
  - type: web
    name: miniblog
    runtime:  java17
    plan: free
    buildCommand: ./mvnw clean package -DskipTests
    startCommand: java -jar miniblog/target/*.jar
    rootDir: miniblog
